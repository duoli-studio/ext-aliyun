# key specification is "namespace::group.key"
system::site.name :
    # type default is string, can not setting by default.
    type : string
    default : 罂粟网络
    description : 网站名称
system::site.is_open :
    type : int
    default : 1
    description : 是否开启
system::site.close_reason :
    description : 站点关闭原因
system::site.copyright :
    description : 版权说明
util::captcha.mail_expired_minute :
    type : int
    default : 15
    description : 邮件验证码有效期(分钟)